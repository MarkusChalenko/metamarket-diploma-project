import { FC } from "react";

import { Error } from "@/components/screens/error/Error";

const Error404Page: FC = (): JSX.Element => {
  return <Error appearance="404" />;
};

export default Error404Page;
