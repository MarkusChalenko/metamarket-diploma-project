import { FC } from "react";

import { Error } from "@/components/screens/error/Error";

const Error500Page: FC = (): JSX.Element => {
  return <Error appearance="500" />;
};

export default Error500Page;
